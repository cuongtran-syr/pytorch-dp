name: fb-dp
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.7
  - numpy>=1.15
  - torchvision>=0.4
  - tqdm>=4.40
  - scipy>=1.2
  - pytest
  - pip
  - tensorboard=1.14.0
  - tensorflow
  - tensorboardX
  - pip:
    - torch>=1.3
    - pytorch-dp
